package search.result

import search.documents.Document
import scala.io.Source

class Snippet(document: Document) {
 
  val sentences = {
    //Source.fromFile(document.file.get).getLines.toList.flatten
  }
  
}